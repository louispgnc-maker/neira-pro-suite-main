/**
 * Test RPC: adapte les paramètres p_cabinet_id / p_document_id si besoin.
 * Utilise dotenv pour charger .env à la racine.
 */
console.error('Sharing disabled: test RPC script is disabled because sharing has been removed from this project.');
process.exit(0);
